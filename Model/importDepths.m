function laminarBoundaryCalculations1 = importDepths(workbookFile, sheetName, dataLines)
%IMPORTFILE Import data from a spreadsheet
%  LAMINARBOUNDARYCALCULATIONS1 = IMPORTFILE(FILE) reads data from the
%  first worksheet in the Microsoft Excel spreadsheet file named FILE.
%  Returns the data as a table.
%
%  LAMINARBOUNDARYCALCULATIONS1 = IMPORTFILE(FILE, SHEET) reads from the
%  specified worksheet.
%
%  LAMINARBOUNDARYCALCULATIONS1 = IMPORTFILE(FILE, SHEET, DATALINES)
%  reads from the specified worksheet for the specified row interval(s).
%  Specify DATALINES as a positive scalar integer or a N-by-2 array of
%  positive scalar integers for dis-contiguous row intervals.
%
%  Example:
%  laminarBoundaryCalculations1 = importfile("S:\formattedDataOutputs\laminarBoundaryCalculations.xlsx", "Sheet1", [2, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 08-Nov-2022 16:52:31

%% Input handling

% If no sheet is specified, read first sheet
if nargin == 1 || isempty(sheetName)
    sheetName = 1;
end

% If row start and end points are not specified, define defaults
if nargin <= 2
    dataLines = [2, Inf];
end

%% Set up the Import Options and import the data
opts = spreadsheetImportOptions("NumVariables", 13);

% Specify sheet and range
opts.Sheet = sheetName;
opts.DataRange = dataLines(1, :);

% Specify column names and types
opts.VariableNames = ["session", "topOfCor", "botOfCor", "SinkBtm", "VarName5", "UpperOutBoundry", "UpperTop", "UpperBtm", "MiddleTop", "MiddleBtm", "LowerTop", "LowerBtm", "LowerOutBoundry"];
opts.VariableTypes = ["string", "double", "double", "double", "string", "double", "double", "double", "double", "double", "double", "double", "double"];

% Specify variable properties
opts = setvaropts(opts, ["session", "VarName5"], "WhitespaceRule", "preserve");
opts = setvaropts(opts, ["session", "VarName5"], "EmptyFieldRule", "auto");

% Import the data
laminarBoundaryCalculations_preCut = readtable(workbookFile, opts, "UseExcel", false);

for idx = 2:size(dataLines, 1)
    opts.DataRange = dataLines(idx, :);
    tb = readtable(workbookFile, opts, "UseExcel", false);
    laminarBoundaryCalculations_preCut = [laminarBoundaryCalculations_preCut; tb]; %#ok<AGROW>
end

laminarBoundaryCalculations1 = laminarBoundaryCalculations_preCut(1:14,:);

end